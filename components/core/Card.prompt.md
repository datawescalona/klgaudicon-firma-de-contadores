Editorial surface card with optional signature tilt and a sticker lift on hover.

```jsx
<Card tilt interactive accent>…</Card>
```

`tilt`: `true` (−1.4°), `"alt"` (+1.2°), or `false`. `interactive` adds hover lift + straighten. `accent` draws a cyan top rule.
