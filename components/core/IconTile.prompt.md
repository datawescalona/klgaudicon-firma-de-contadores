Soft rounded-square tile holding a 2px line-art icon, with the brand "sticker" hover shadow.

```jsx
<IconTile tone="cobalt" icon={<i data-lucide="shield-check"></i>} />
```

Tones: `cobalt` (default), `cyan`, `bronze`, `ink`. Pair with `ServiceCard` or stat blocks.
