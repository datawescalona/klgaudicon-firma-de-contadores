Compact uppercase mono label for service categories, statuses, and tags.

```jsx
<Badge tone="bronze" dot>Respuesta 24h</Badge>
```

Tones: `cobalt` (default), `cyan`, `bronze`, `ink`, `success`, `solid`. Set `dot` for a status indicator.
